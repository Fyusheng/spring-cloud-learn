server:
  port: 8765

spring:
  application:
    name: service-feign

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

#开启Feign对Hystrix的支持
feign:
  hystrix:
    enabled: true
